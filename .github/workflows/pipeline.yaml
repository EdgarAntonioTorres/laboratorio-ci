me: Pipeline de prueba

on:
  push:
    branches: [ main ]
  
  jobs:
    mensaje:
      runs-on: ubuntu-latest

      steps:
        - name: Descargar repositorio
          run: actions/checkout@v3

        - name: Ejecutar mensaje
          run: echo "Pipeline ejecutado correctamente en Github Actions"